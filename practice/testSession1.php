<?php

session_start(); //means starting a session. can put this in a master include file, so it starts always

//check if logged in already, if are, redirect to home or whatever.

$_SESSION['UserID'] = 4;


?>
